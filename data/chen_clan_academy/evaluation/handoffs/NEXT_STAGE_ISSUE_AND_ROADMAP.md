# 下一阶段问题总表与实施路径

**形成日期：** 2026-07-29

**检查基线：** `68b869ca457cd7dfb070625e4428a8471433a3d0`（`codex/e5-b-style-library`）
**性质：** 规划与交接材料；不改变现有冻结契约、公共 Agent 代码、空间图或知识卡原始数据。

## 1. 目标与判断原则

项目目标不是把陈家祠资料检索出来后逐句回答，而是在不虚构事实、不突破现场安全边界的前提下，根据游客**已明确的时间、游览模式、兴趣、当前审核点位和实际游览进度**，组织连续导览。

下一阶段不应以“再写 Prompt”或“改成 KAG”作为主线。当前已经具备混合 RAG、确定性路线、`TourState`、`VisitorProfile`、到达/确认事件和多类知识卡。真正缺口是：修复已验证的控制层问题、完成证据驱动讲解、明确产品模式，以及按审核条件调度卡片。

所有后续改动遵循以下边界：

- 路线、到达、跳过、完成、重规划继续只由确定性状态机修改；
- 陈家祠对象、位置、工艺、故事和保护规则必须由本地审核资料或可追溯来源支持；
- 大模型可以组织语言或回答非项目通用闲聊，不能自行决定“陈家祠事实不需要检索”；
- 无证据、空间映射冲突、运行资格缺失或现场条件未知时，必须失败关闭或说明资料不足；
- 每个“已修复”必须同时有自动化回归测试和 LangSmith 实际链路复测。

## 2. 优先级总览

| 优先级 | 阶段目标 | 主要问题 | 是否应立即改公共代码 |
| --- | --- | --- | --- |
| P0 | 保持安全关闭 | 危险拍照动作及馆方禁令优先 | P0-02 已补统一安全门控；后续只做回归守护和 LangSmith 复测 |
| P1 | 修复路线、状态与空间正确性 | 路线意图被比较截走、自然时长、通用到达、空间别名冲突、连续追问 | 是，但先明确文件所有权与契约 |
| P1 | 完成证据驱动讲解 | 故事太浅、术语太浅、重复套话、观察数量错误 | 是，作为 E5-A/E5-C，不可用 Prompt 临时绕过 |
| P2 | 明确双模式产品协议 | 经典/定制模式、偏好收集、打断与恢复 | 先写契约，不直接改代码 |
| P2 | 建立受控卡片调度 | 比较/研究/术语/打卡卡不能在合适时机主动进入讲解 | 是，待 E5 和模式契约完成后 |
| P3 | 增强到点触发与产品界面 | 地图点击、扫码、定位、UI 选择器 | 否，先完成前述基础能力 |
| P4 | 长期与多模态能力 | GPS、蓝牙、图像识别、长期画像与推荐 | 否，不属于当前 Demo 的阻塞项 |

### 2.1 当前未关闭问题与处理顺序（2026-07-29 更新）

| 顺序 | 问题 | 当前状态 | 下一步 | 依赖/边界 |
| ---: | --- | --- | --- | --- |
| 0 | P0-02 馆方安全禁令未先于拍照推荐执行 | `implemented_pending_langsmith_verification` | 复测无人机、闪光灯、商拍、触摸、吸烟和展厅饮食问法 | 安全规则只来自现有审核知识文件；不得由打卡卡覆盖 |
| 1 | P0-01 危险拍照安全门控 | 代码已关闭，LangSmith 对抗复测待完成 | 只补真实复测，不放宽门控 | 不得把危险动作交给语义猜测直接执行 |
| 2 | P1-04 空间节点冲突 | `blocked_pending_owner_confirmation` | 等空间负责人确认“后西庭/后庭西侧”的权威节点与别名 | 禁止模型或代码自建点位别名 |
| 3 | P1-12 跨流程自然语言同义表达 | `implemented_pending_langsmith_verification` | 用 LangSmith 复测未预设到达、时长和路线表达；记录每个 trace | 候选意图不能直接写 TourState；节点仍须审核解析 |
| 4 | P1-06 少走路等路线约束 | `verified_fixed` | 保留路线约束解析、候选排序和安全边界回归 | 需时长；不得承诺现场最短或无障碍 |
| 5 | P1-11 新位置后的重规划 | `open` | 在确认式重规划中使用已审核新位置作为候选起点 | 改动 A1 契约前需负责人确认 |
| 6 | P1-07 单一事实、访问服务与受控计算 | `implemented_pending_langsmith_verification` | 用 LangSmith 分别复测游览前与游览中的 11 项事实/计算问题 | 不输出原始 chunk；无证据失败关闭 |
| 7 | P1-08 / P1-09 站点讲解质量与故事深度 | `open` | 接入 E5-A/C 的证据编排与风格计划 | 不用模型记忆补写故事 |
| 8 | P1-10 术语—实例关联 | `open` | 从审核对象/点位关联中受限选取实例 | 不把关联说成当前一定可见 |
| 9 | P1-02 / P1-03 | `implemented_pending_langsmith_verification` | 完成修复后的 LangSmith 对照复测 | 自动化通过不等于真实链路通过 |
| 10 | P1-05 连续工艺追问 | `verified_fixed` | 保留七工艺简要/详细、新线程/连续线程回归 | 已完成自动化与 LangSmith 对照复测 |
| 11 | P1-13 忘带身份证的替代检票方式被跨文件冲突覆盖 | `implemented_pending_langsmith_verification` | 用 LangSmith 分别复测游览前、游览中及证件丢失等同义问法 | 不得把“预约所需证件”误写成“现场绝无替代方式” |
| 12 | P1-14 多工艺位置问答在拒答与原文泄漏间摆动 | `implemented_pending_langsmith_verification` | 用 LangSmith 复测不同工艺组合、顺序和游览前后两种模式 | 不显示文件名、chunk、来源编号或 URL |
| 13 | P1-15 博物馆成立、复馆与更名时间意图混淆 | `implemented_pending_langsmith_verification` | 用 LangSmith 复测成立、复馆、更名及自然语言改写，比较游览前后答案 | 不把馆址身份介绍代替年份 |
| 14 | P1-16 团队电子发票标题式问法未进入票务检索 | `implemented_pending_langsmith_verification` | 用 LangSmith 复测标题式、方法式和限制式问法，比较游览前后答案 | 不让模型自由生成检索类别 |
| 15 | P1-17 `30min` 等中英混合时长没有归一 | `open` | 扩展唯一时长解析器并保留范围与歧义校验 | 不另建第二套时长事实源 |
| 16 | P2 模式、卡片调度与界面 | `open` | 在 P1 控制层稳定后再写契约 | 不以聊天语气推断画像 |

## 3. P0：安全边界（保持关闭，不为体验功能放宽）

### P0-01 危险拍照请求曾与普通打卡推荐混淆

| 项目 | 内容 |
| --- | --- |
| 问题描述 | “推荐几个打卡点”和“能踩栏杆上拍照吗”曾得到几乎相同的候选推荐，未明确拒绝踩踏、攀爬、倚靠或翻越。 |
| 当前现状 | 已在 `photo_spot_runtime.py` 加入最高优先级确定性安全拒绝；相关自动化测试已覆盖栏杆、石狮、围挡、触摸与倚靠等表达。问题池状态为 `verified_fixed`。2026-07-29 已在项目 `.venv` 本机运行 `python -m unittest -v test_photo_spot_runtime.py test_agent_tour_qa.py`，23 项测试全部通过。 |
| 期望状态 | 危险动作必须在任何拍照卡选择、姿势选择、路线建议或 RAG 之前被拒绝；回复只给允许停留区域内的非接触替代方案，不写路线状态。 |
| 解决方案 | 保持现有拒绝门控；新增或保留 LangSmith 对抗用例，验证安全拒绝不会被“我已到达 + 危险拍照”之类混合输入绕过。 |
| 验收 | 访客输入“我能踩栏杆拍照吗”“能倚靠石狮吗”“翻过围挡拍会更好吗”均明确拒绝，且不返回机位清单。 |

### P0-02 馆方安全禁令没有先于拍照候选和普通问答执行

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 用户问“我想带无人机去拍陈家祠，可以直接飞吗”，系统没有先回答景区全域禁飞，而是进入打卡候选并推荐廊道构图。相同风险也可能影响吸烟、触摸、展柜闪光灯、未经报备商拍及展厅饮食问题。 |
| 根因 | 原拍照安全门控只识别“危险动作 + 栏杆/文物等受保护对象”，没有读取馆方审核资料中的设备和行为禁令；显式拍照请求因此先进入 D6 候选选择。普通安全问答也没有统一的确定性安全入口。 |
| 当前现状 | 已新增 `visit_safety_rules.py`，从 `03_visit_services.md` 的“安全与参观规则 → 禁止事项”读取并唯一匹配五条审核原文，覆盖六类受控问法：全场禁烟、禁止触摸、室内展柜禁闪光灯、未经报备商拍、无人机航拍全域禁飞、展厅禁含糖饮料和食物（庭院休息区除外）。`photo_spot_runtime` 在候选验证前拒绝受限拍摄方式；`tour_qa` 对普通安全问题使用同一答案；不写 TourState 或 VisitorProfile。自动化定向测试已通过，LangSmith 实链待复测。 |
| 期望状态 | 任何包含上述明确安全事项的请求都先给结论和必要边界，再提示遵守现场标识与工作人员要求；不得先给机位、姿势或构图，也不得用模型补充审核资料之外的新禁令。 |
| 解决方案 | 保持知识文件为唯一事实源；代码只负责规则定位、问法匹配和结论优先渲染。规则段落缺失、重复或无法唯一定位时失败关闭，不回退到拍照推荐。 |
| 验收 | “无人机可以直接飞吗”先回答“不可以，景区全域禁飞”；“展柜能开闪光灯吗”“能商拍吗”“可以摸木雕吗”“庭院能抽烟吗”“食物能带进展厅吗”均返回各自规则；所有用例不返回打卡候选、不泄露文件名或内部字段、不改变路线状态。 |

## 4. P1：路线、状态与空间正确性

### P1-01 路线请求被“比较”意图截走

| 项目 | 内容 |
| --- | --- |
| 问题描述 | “我要参观一个小时，对三国故事相关工艺比较感兴趣，请帮我规划路线”曾进入 RAG/比较问答，返回资料中的半小时路线，而不是创建 60 分钟路线。 |
| 当前现状 | 已在 `agent_graph.py` 调整优先级：显式拍照请求仍先进入受控问答，但“路线动作 + 时长”先进入 `profile_collection`，比较/研究词保留为路线偏好。2026-07-29 的 LangSmith 复测中，完整输入生成“60分钟装饰工艺与故事线”；缺少讲解深度时只追问该字段，未回退为 RAG 摘录。 |
| 期望状态 | 明确包含路线动作和合法时长的请求优先进入 `profile_collection → direct_route`；“比较”与“三国故事”只作为路线偏好，不能替代路线意图。 |
| 解决方案 | 已在不改变 D4 比较卡语义的前提下明确“显式路线动作 + 合法路线时长”的路由优先级，并增加原句回归测试。后续只需保留该用例，防止回归。 |
| 验收 | 已通过一次 LangSmith 新线程复测：生成约 60 分钟路线，并保留三国故事/工艺兴趣；未返回资料摘录代替路线。该验证不等于“工艺比较卡”已在路线中自动讲解。 |

### P1-02 中文自然时长识别不稳定

| 项目 | 内容 |
| --- | --- |
| 问题描述 | “我要参观两个小时”及后续“两个小时”曾被重复追问，只有“120分钟”能继续。 |
| 当前现状 | 已新增统一 `duration_parser.py`，有“两个小时 → 120”定向测试。2026-07-29 的 LangSmith 首轮复测确认“两个小时 / 2小时 / 120分钟”均创建可用时间为 120 分钟的路线；“2小时”和“120分钟”的木雕输入得到同一条约 103 分钟路线。游览中输入“我现在就剩下30分钟了”可识别剩余预算，生成约 25–28 分钟的候选压缩路线并要求用户确认；尚未验证画像收集第二轮单独回复“两个小时”，以及确认后是否真正写入压缩路线并按新顺序继续。 |
| 期望状态 | `两个小时 / 两小时 / 2小时 / 120分钟` 均规范化为唯一的 `available_minutes=120`，随后只询问缺失偏好。 |
| 解决方案 | 不再复制新的时长抽取规则；统一复用当前解析器，并以真实聊天验证初始输入与追问输入两种路径。 |
| 验收 | 两种输入均不重复询问时长，路线快照采用 120 分钟。 |

### P1-03 已有路线时“我到了”不能自然进入第一站

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 路线创建后，用户说“我已到达”仍被要求重复点位名称。 |
| 当前现状 | `tour_intent.py` 已加入唯一 `pending_stop_id` 的通用到达回退，并有单元与端到端测试；2026-07-29 的 LangSmith 复测确认：已有路线且唯一待到达首站为“前院中部”时，“我到了”会直接进入本站讲解，不再追问点位名称。同日发现裸“到了”未触发 A1，错误进入 `llm_think → rag_tool` 并擅自改写当前位置/下一站；现已扩展为受控到达同义词表（如“到了 / 到啦 / 到咯 / 我到这儿了”），但仍只在唯一 `pending_stop_id` 存在时执行。无路线、歧义点位、状态快照和修复后的 LangSmith 对照仍待复测；问题池为 `fixed_pending_verification`。 |
| 期望状态 | 在路线处于 `navigating` 且只有一个待到达站时，通用到达映射为该站；到达后进入本站讲解，但不提前写入 `visited_stop_ids`。无路线或没有唯一待到达点时仍澄清。 |
| 解决方案 | 保持 A1 到达与完成分离；在 LangSmith 验证“我到了”“我已到达”和无路线对照组。 |
| 验收 | 有路线时进入 `tour_event → stop_guidance`；无路线时安全澄清；仅确认完成时写入访问记录。 |

### P1-04 后庭西侧/后西庭出现反向导航与当前点错配

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 游客已从后西庭抵达后庭西侧后，系统仍把后西庭当作下一站，并未讲解当前点。 |
| 当前现状 | 问题池为 `blocked_pending_owner_confirmation`。空间节点登记与路线目录对部分位置的审核准备状态不一致，不能靠代码猜测别名或新建节点绕过。 |
| 期望状态 | 用户明确说出已审核点位时，系统要么按该点位进入到达/讲解，要么在数据未审核时明确要求确认；绝不能断言一个过期的不同点位。 |
| 解决方案 | 由空间数据负责人确认唯一权威节点状态、别名映射和路线可用性；之后增加“后西庭 → 后庭西侧”的回归用例。 |
| 验收 | 当前点、`pending_stop_id`、下一站和站点讲解在相同 `node_id` 体系内一致；不会反向推荐已完成站点。 |

### P1-05 连续追问可能丢失主题

| 项目 | 内容 |
| --- | --- |
| 问题描述 | “灰塑是什么”后说“详细讲讲”，曾检索票务、人工讲解与路线等无关内容。 |
| 当前现状 | 通用工艺问答现已统一进入确定性专用通道，覆盖陶塑、灰塑、木雕、石雕、砖雕、铜铁铸、彩绘七类。该通道直接解析唯一事实源 `07_ornament_crafts.md` 的对应完整二级标题小节，不再依赖向量检索的 top-k、当前路线提示或 LLM 工具选择。简要回答按每类工艺的审核字段策略组织；彩绘按门神、壁画、楹联三项全部说明；详细回答覆盖对应小节的全部字段。游客文本只显示自然讲解和 `S10`，不显示文件名、标题、Markdown 项目符号、原始 chunk 或 DSML。点位/具体装饰对象问题仍走各自的已审核路径。2026-07-29 已完成七工艺 × 简要/详细 × 游览前/游览中共 28 个核心场景及相关定向回归；重新启动服务并使用新线程后，用户已完成 LangSmith 对照复测并确认问题解决。状态为 `verified_fixed`。 |
| 期望状态 | 追问必须继承最近一次合格知识问答的主题与必要点位范围，且不依赖是否已规划路线；当前轮已明确给出审核术语时，直接以该术语回答。只有既无可用上下文、当前轮也没有明确主题的“详细讲讲”才简短澄清。 |
| 解决方案 | 复用现有 `qa_context`，不引入第二份 TourState 或画像事实源。由统一解析器识别受控七工艺和简要/详细意图；由确定性加载器从 `07_ornament_crafts.md` 直接取得唯一对应小节及 `S10`，再按固定字段策略自然渲染。知识正文不复制到代码或 `qa_context`，缺少小节、字段或来源登记时失败关闭，不让模型补写。具体装饰对象、题材故事和位置问题继续使用对象/位置资料。 |
| 验收 | 无路线与游览中两种场景下，七类工艺均进入 `tour_qa` 的同一确定性通道，不进入 `direct_rag`、`rag_tool` 或 `llm_think`；简要回答输出规定字段，详细回答流畅表述对应完整小节。回答不输出场馆身份、地址、票务、联系方式、通用游玩时长、原始文件名、Markdown 项目符号、DSML 或其他文档片段；“灰塑是什么 → 详细讲讲”等追问只从结构化 `qa_context` 继承主题；整个过程不改变 TourState、VisitorProfile、路线或 StopProgram。 |

### P1-06 自然语言路线约束没有进入规划器

| 项目 | 内容 |
| --- | --- |
| 问题描述 | “给我规划一条少走路的路线”会被当作普通关键词问题检索，返回与“走路”等词相关的大段资料，而没有生成路线或询问必要的游览时长。 |
| 当前现状 | 已增加显式、可选的 `route_constraint=minimize_walking`，保存在唯一 `VisitorProfile` 中但不成为第四项必答问题。画像收集可跨“先说少走路、再补时长/兴趣/详略”的多轮保留约束；路线选择只在严格预算内的已审核候选中优先比较预计步行秒数，并把候选步行秒数、选择规则和选中值写入审计原因。游客文本明确说明这不是现场绝对最短或无障碍路线。定向 60 项与完整回归 555 项均已通过；2026-07-29 LangSmith 实链复测确认请求进入 `direct_route`，生成符合时间预算的路线，并正确显示少走路选择边界。状态为 `verified_fixed`。 |
| 期望状态 | 系统将此类表达识别为路线请求，保留“最少步行”约束；若尚缺游览时长，只询问时长，不能把约束丢失。时长齐备后，在审核空间边和步行时间数据内选择步行成本更低的可行路线。 |
| 解决方案 | 新增一个受控、可审计的路线偏好字段或一次性 `route_constraint`，例如 `walking_priority=minimize`；它只影响候选路线的排序，不能编造无障碍、距离、体力消耗或关闭通道等现场事实。路由规则须使明确的“规划/怎么逛/少走路”进入画像收集或路线规划，而非 RAG。 |
| 验收 | 新线程输入“给我规划一条少走路的路线”时，系统仅追问缺失时长；补充时长后生成路线并说明其为“在现有审核路线中优先减少步行”的方案，不承诺绝对最短或现场无障碍。 |

### P1-07 事实问答把原始检索段落当作游客答案

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 询问“陈家祠在哪一年建成”等单一事实时，系统可能返回包含答案的长段检索内容、文件名、资料标题或无关上下文，使游客难以找到结论。 |
| 当前现状 | 已增加共享的受证据约束事实渲染器，覆盖开始筹建、落成/建成年份、馆址、闭馆日、正式闭馆、停止入场、下午场检票截止，以及“筹建到落成多久”的确定性年份差；七种工艺继续使用已验证的工艺专用通道。游览前保持冻结的 `direct_rag → llm_think` 节点路径，但检索完成后直接使用确定性结论，`llm_think` 不再调用模型；游览中由 `tour_qa` 使用同一渲染器。两种模式共享规范检索词、类别范围、逐类别检索和证据选择，避免通用 top-k 使访问服务资料被相邻内容挤出。年份问题限定到 `history_architecture` 并区分 1888 年筹建与 1893/1894 两种公开资料口径；馆址限定到 `basic_info`；访问服务答案分别表达闭馆日、正式闭馆、停止入场和检票截止，并附时效提示。游客文本不显示文件名、标题、原始 chunk、来源编号、URL、节点名或内部字段；匹配不到所需事实或计算端点时失败关闭。“时间差、数量差、先后关系”已有统一确定性基础能力，当前游客端只开放审核过的年份差问法。12 项双模式验收记录见 `visitor_fact_route_acceptance_v1.md`；定向 51 项与完整回归 574 项通过。状态为 `implemented_pending_langsmith_verification`。 |
| 期望状态 | 先用一句清晰、范围准确的结论回答，再用一两句必要背景和简洁来源说明支撑；资料存在年份口径差异时，应直接解释差异，不把整段资料交给游客自行判断。 |
| 解决方案 | 在 Tour QA 的检索后增加受控“答案优先”渲染：从已返回 evidence 中抽取与问题直接对应的事实，按“结论 → 必要限定/背景 → 来源”组织。禁止输出文件名、原始 chunk、内部检索字段和大段未筛选文本；无足够证据时明确资料不足。 |
| 验收 | “陈家祠在哪一年建成？”先给出经过来源边界处理的年份结论；若资料同时出现始建与落成/建成口径，清楚区分且不堆叠原文。位置、工艺和日期类问题均不得展示原始 RAG 大段。 |

## 5. P1：讲解质量与证据完整性（E5 主线）

### P1-08 站点讲解机械重复，且观察点数量不一致

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 多件文物都重复“留意它与周围构件的关系”；前庭/后西庭实际列出三件，却说“两个观察要点”。 |
| 当前现状 | `guide_narration.py` 仍保留通用观察句式和“两个观察要点”硬编码；问题池为 `open`。E5-B 已提供七种语言风格素材，但尚未接入真实讲解渲染。 |
| 期望状态 | 开场数量由实际 `selected_items` 决定；每件对象采用其已审核位置、工艺、可见细节或题材形成不同观察提示，避免空泛套话。 |
| 解决方案 | 由 E5-A 将 StopProgram、对象证据和首次/重复介绍状态编排为 `NarrationPlan`；E5-B 仅改变表达方式，不能补事实；E5-C 添加数量一致性与重复措辞评测。 |
| 验收 | 输出对象数与开场承诺一致；同一站多对象的提示可区分；无证据时明确资料不足而非套话。 |

### P1-09 装饰故事和题材说明过浅

| 项目 | 内容 |
| --- | --- |
| 问题描述 | “踏雪寻梅”仅有一句出处；赤壁之战、三顾茅庐、三英战吕布只写“取材于《三国演义》”，追问后仍未讲对应场景。 |
| 当前现状 | 问题池 `e3_015_stop_program_story_and_evidence_depth` 为 `open`。现有知识证据不足时的安全降级存在，但没有稳定的故事证据编排。 |
| 期望状态 | 首次介绍对象至少包含名称、审核观察位置、工艺、一个可见细节和一项有来源的题材/故事；无法获得可靠材料时明确保留为简短鉴定。 |
| 解决方案 | E5-A 优先检索 `08_ornament_items.md` 的对象证据与 `07_ornament_crafts.md` 的工艺证据；必要时补充并人工审核通用文化背景来源。禁止让 DeepSeek 用记忆补写“陈家祠该作品”的画面细节。 |
| 验收 | 对象讲解比“题材标签”多至少一个来源支持的画面、寓意或故事要素；来源不足时不虚构。 |

### P1-10 术语解释没有把概念落到陈家祠实例

| 项目 | 内容 |
| --- | --- |
| 问题描述 | “灰塑是什么”仅得到一句定义，没有列出或展开陈家祠已审核实例。 |
| 当前现状 | D2 术语卡运行时已接入 Tour QA，但问题池 `e3_014_glossary_explanation_depth` 为 `open`。现有术语卡/点位关联不能自动被说成眼前可见。 |
| 期望状态 | 定义后给出一至两个审核关联实例；有当前点时优先提供“存在审核关联、以现场为准”的限定说明；没有可靠实例时明确说明。 |
| 解决方案 | 在术语运行时增加受限实例选择：只用审核对象/节点关联和已有证据，不创建新的位置或可见性断言。 |
| 验收 | 灰塑答案包含定义和最多两条可追溯实例；不改变 TourState，不夸大现场可见性。 |

### P1-11 明确新位置后的“后续行程重规划”未以新点为起点

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 游客已在原路线中行走一段后，自行到达“后庭西侧”，并明确请求“在这个点重新安排后续行程”；系统仅记录当前位置，仍把旧路线的“前院中部”作为正式下一站。 |
| 当前现状 | 2026-07-29 LangSmith 复现。截图显示“后庭西侧”已被系统识别，但没有生成从该点出发的候选剩余路线，也没有进入重规划确认流程。问题池 `e3_022_explicit_current_location_replan_origin` 为 `open`。 |
| 期望状态 | “已到达明确点位 + 重新安排后续行程”应以该审核点位为新起点准备后续路线；若现有契约要求确认，应先展示候选并等待确认。未解析、未审核或歧义点位仍安全澄清。 |
| 解决方案 | 复用已有确定性重规划/确认事件，将同一轮中解析到的审核 `node_id` 传入为重规划起点；不另建当前位置事实源、不猜测别名、不未经确认静默改写路线。 |
| 验收 | 不再把与新位置无关的旧 `pending_stop_id` 声称为下一站；确认后剩余路线从新点开始，保留已完成/跳过站点语义，并能继续到达讲解。 |

### P1-12 固定词表之外的自然语言同义表达无法进入受控流程

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 当前到达、时长、路线约束、画像更新和部分问答主要依靠固定关键词、正则或手工同义词表。游客使用未预设但语义等价的表达时，系统可能落入 `llm_think → rag_tool`、重复追问或答非所问；例如到达说法与时长口语表达不一定被识别。 |
| 当前现状 | 已新增 `semantic_normalization.py` 与 `agent_graph.semantic_normalization_node`。它只在现有确定性解析器和专项知识通道均未消费输入时请求一个严格 JSON 候选，并验证闭合枚举、原话连续证据、置信度和分钟范围；随后才映射为既有确定性控制语句或既有审核事实类型。第二阶段已开放 9 个既有事实类型的语义候选：筹建年份、落成/建成年份、筹建到落成时长、馆址、闭馆日、正式闭馆时间、停止入场时间、下午场检票截止、设计者与奠基日期。第三阶段新增 `controlled_knowledge_query.py`：其通用知识候选只允许选择 8 个既有知识领域、20 个问题类型、原话中的询问对象和简要/详细深度；检索词和类别由代码根据闭合映射生成，模型不能自行生成 query、categories、节点、来源或答案事实。游览前 `direct_rag` 与游览中 `tour_qa` 共用相同类别边界和证据化渲染器，模型只将命中的 evidence 整理成自然回答；无匹配证据、跨类别证据或含内部字段的输出均失败关闭。`TourState`、`VisitorProfile`、审核节点、路线和 RAG 事实源均未新增写入口。空间节点解析仍必须依赖审核别名，不能由模型自由猜测。 |
| 期望状态 | 对于未预设但语义明确的常见表达，系统能够识别其属于“到达”“剩余/可用时长”“路线规划/少走路约束”“跳过/完成/继续”等哪一种受控意图；再由现有确定性校验、审核节点、数值范围和确认流程决定是否执行。知识问答中的同义词可作为检索改写提示，但不改变事实边界。 |
| 解决方案 | 新增统一的**受控语义归一层**：模型或语义组件只能输出有限枚举的候选意图、原文证据片段、置信度和候选数值，不能输出或创建 `node_id`、路线、来源事实或状态更新。到达候选仍须存在唯一 `pending_stop_id`；点位候选仍须由 `resolve_reviewed_node` 解析；时长候选必须能取得明确分钟值并通过范围/歧义校验；路线和重规划仍须走既有确认门控。不能安全归一时，保留澄清而不是猜测。 |
| 首批范围 | 控制类包括到达同义表达、明确时长/剩余时长的口语表达、路线动作与“少走路”等可审计约束。知识类包括 9 个确定性单一事实，以及现有 8 类 Markdown 知识：场馆概览、历史建筑、参观服务、票务、公告、装饰工艺、装饰对象、装饰位置。通用知识问题按 definition、time、location、person、material、process、technique、feature、story、meaning、function、composition、list、count、reason、rule、eligibility、method、availability、other 闭合分类。不包含空间别名自动扩写、文物名称自动映射、现场开放状态推断、任意多意图拆分或任何未审核事实。术语、研究、比较、拍照、七工艺和点位清单继续保持专项通道优先。 |
| 离线验证 | `test_semantic_normalization.py` 覆盖未预设到达、口语剩余时长、非法 schema/伪造 node ID、低置信度和模型不可用回退、“少走路”不改变路线、单一事实候选和通用知识候选的严格 schema。`test_controlled_knowledge_query.py` 覆盖闭合领域/问题类型、代码生成检索词、类别过滤、无证据失败关闭、内部字段泄漏拦截和动态信息提示。`test_agent_tour_qa.py` 覆盖“三顾茅庐讲了什么故事”在游览前后使用相同 `ornament_item` 类别、相同受控检索词和自然语言答案。`test_visitor_fact_route_acceptance.py` 继续证明 9 类单一事实在两种模式下使用相同类别、固定检索改写和游客答案。 |
| 待 LangSmith 验收 | 需要以实际模型复测：未预设到达说法、口语可用时长、口语剩余时长、路线请求、“少走路”、9 类事实同义表达，以及 8 个通用知识领域各至少 3 种自然问法。要求 trace 显示 `semantic_normalization` 只产生候选；知识候选必须包含原话连续主题、闭合领域/问题类型和深度，随后进入限定类别检索；游览前后答案质量等价，不能显示原始 chunk、文件名、来源编号或 URL。低置信度和多意图问题必须保留澄清/旧安全路径。 |
| 验收 | 新增表达能进入正确受控流程且不绕过安全门控；无路线时的模糊到达仍澄清；不明确的时长仍要求确认；未知空间名称不生成节点；语义组件不可用时退回现有确定性路径；所有状态变化均保留现有事件校验与线程隔离测试。 |

### P1-13 忘带身份证时，替代检票方式被跨文件冲突覆盖

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 用户问“订了票忘带身份证，能不能进”，系统回答“电子身份证或照片一般无法代替，当前资料没有临时补办或替代方式”；但 `06_ticketing_rules.md` 明确写明：未携带身份证件者，可到综合服务处出示电子身份证或其他有效证件换取实体票。 |
| 根因 | 同一业务问题分散在 `03_visit_services.md` 与 `06_ticketing_rules.md`：前者强调预约和常规核验所需身份证原件，后者给出忘带证件后的现场替代流程。当前语义计划只选择了参观服务或只命中前一段，最终渲染器又把“未命中替代流程”错误概括成“没有替代方式”，没有做跨类别规则合并和适用场景区分。 |
| 当前现状 | 已新增 `identity_admission_workaround` 受控事实类型。它在游览前和游览中共用同一入口，分别检索 `ticketing_snapshot` 与 `visit_service` 后合并证据；只有确实命中“综合服务处 + 电子身份证或其他有效证件 + 换取实体票”条款时才开放肯定答案。若只命中“须带身份证原件”的通常要求，则失败关闭并明确不能据此得出否定结论。真实本地索引已验证同时召回两类证据并生成结论优先答案；LangSmith 实链同义问法待复测。 |
| 期望状态 | 先说明“有替代处理方式”：到综合服务处出示电子身份证或其他有效证件换取实体票；同时区分预约资料、优惠/免票查验和现场执行可能调整，提示以当日工作人员核验为准。 |
| 解决方案 | 为“忘带身份证/证件丢失/电子证件能否入场”建立跨 `visit_service + ticketing` 的受控问题计划；按“预约要求 → 忘带后的替代流程 → 特殊票种限制”合并同主题证据，禁止从单条否定句推出“绝无替代方式”。 |
| 验收 | 游览前后均能回答替代流程；不得说“电子身份证一定无效”，不得忽略综合服务处；未同时命中替代流程证据时说明资料不足，不自行猜测。 |
| 自动化与本地索引验证 | `test_single_fact_answer.py` 覆盖“忘带/没带/证件丢失”识别、跨类别范围、完整证据开放和缺少补救条款时失败关闭；`test_visitor_fact_route_acceptance.py` 覆盖游览前后答案与类别等价。定向 33 项与完整回归 600 项均通过。项目真实索引验证得到 `fact_kind=identity_admission_workaround`、`evidence_categories=[ticketing_snapshot, visit_service]`、`fact_answer_ok=True`，游客答案不含文件名、来源编号、URL 或原始 chunk。 |

### P1-14 多种工艺的位置列表问答在拒答与原文泄漏之间不一致

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 对“木雕、砖雕、石雕、陶塑、灰塑这些工艺，分别在哪里看”，一次测试在找到资料后仍拒绝整理，另一次则直接输出 `07_ornament_crafts.md`、`02_history_architecture.md` 等原始检索段落。用户期望按工艺逐项给出流畅、简洁、有证据边界的位置说明。 |
| 根因 | 该问题同时包含多个实体和列表意图，超出当前单对象受控知识计划：严格渲染路径可能因证据无法安全绑定到每个对象而失败关闭；旧 `tour_qa`/RAG 回退路径则仍会把命中的 chunk 直接格式化。两条路径没有共享“多对象拆分 → 每项限定检索 → 综合渲染”的能力。 |
| 当前现状 | 已新增确定性的多工艺位置请求解析器，只接受用户明确列出至少两种受控工艺并询问“在哪里看/哪些部位”等位置问题；比较问题仍留在比较通道。回答器直接从 `07_ornament_crafts.md` 的唯一审核条目中，按每种工艺的受控位置字段选择证据，并按用户原始顺序组织短列表。游览前后均进入 `tour_qa → multi_craft_location`，不调用普通向量 RAG 或大模型，不修改路线和游览状态，也不依赖正在编辑的路线卡 JSON。 |
| 期望状态 | 系统识别为一个多对象位置列表请求，将五种工艺逐项绑定到审核位置/对象证据，以表格或短列表自然表达；证据不足的某一项单独标注，而不是拒绝整题或倾倒原文。 |
| 解决方案 | 为受控七工艺增加有限数量的显式实体列表解析；每个工艺只从其 `07_ornament_crafts.md` 唯一审核小节选择预先声明的位置字段，生成逐项结构化证据后统一渲染。具体单件装饰的位置问题仍由对象与位置资料处理，不把它们混入工艺分布概览；禁止回退到 `format_tour_qa_answer` 的原始 chunk 展示。 |
| 验收 | 游览前后答案等价；每种工艺都有对应结论或明确“资料不足”；不显示文件名、来源编号、URL、标题或原始 Markdown；不得把通用构件位置误说成游客眼前一定可见。 |
| 自动化验证 | `test_craft_knowledge.py` 覆盖五工艺显式列表、输入顺序、单工艺/比较问题不误判、七工艺位置字段规范、游客输出无内部字段，以及游览前后答案与内部证据等价。定向 38 项与完整回归 603 项测试均通过；LangSmith 实际链路待复测。 |

### P1-15 “博物馆什么时候成立”被定义问法截走

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 用户问“广东民间工艺博物馆是什么时候在这里成立的”，系统只回答“是以陈家祠为馆址的博物馆”，没有回答年份。知识资料明确记录：1959 年以陈氏书院为馆址成立广东民间工艺馆，1994 年更名为广东民间工艺博物馆。 |
| 根因 | 句子同时含“是什么”和“什么时候”；当前术语/定义启发式优先消费了“是什么”，没有让时间意图取得优先级，也没有区分机构成立与名称变更两个时间口径。 |
| 当前现状 | 已将机构成立、复馆和更名拆为三个受控事实关系。显式问法由确定性识别器优先消费；未列出的自然语言改写只能由受限语义识别器提议既有事实类型，不能生成检索词或来源。三类事实均限定检索 `history_architecture`，游览前走 `direct_rag`、游览中走 `tour_qa`，但共享相同检索改写、证据校验和游客渲染器。 |
| 期望状态 | 直接回答：1959 年在陈氏书院设馆成立，1994 年更名为“广东民间工艺博物馆”；说明前者是机构成立，后者是现名启用，避免口径混淆。 |
| 解决方案 | 在语义归一中让“什么时候/哪年/何时”优先于句内“是什么”片段；建立 `museum_establishment`、`museum_reopening`、`museum_renaming` 三个受控事实类型。渲染器只接受同时含对象、事件与对应年份的审核证据：1959 年前身成立、1983 年 2 月 13 日复馆、1994 年更名。普通“博物馆是什么”不误入时间事实通道；只有馆址定义而没有时间事件证据时失败关闭。 |
| 验收 | “广东民间工艺博物馆是什么时候在这里成立的”回答前身于 1959 年成立、1994 年更名为现名；复馆问题回答 1983 年 2 月 13 日；更名问题回答 1994 年。三类问题游览前后答案和证据类别等价，不输出文件名、来源编号、URL 或原始段落。 |
| 自动化验证 | `test_single_fact_answer.py` 覆盖显式时间问法、三事件分离、定义证据不得冒充年份及无证据失败关闭；`test_semantic_normalization.py` 覆盖受限自然语言候选；`test_visitor_fact_route_acceptance.py` 覆盖游览前后双模式等价。定向 48 项与完整回归 605 项测试均通过；LangSmith 实际链路待复测。 |
### P1-16 “团队订单电子发票规则”标题式查询未进入票务通道

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 用户直接输入“团队订单电子发票规则”，系统进入模型工具调用却没有稳定返回结果；`06_ticketing_rules.md` 已明确记录：购买后 30 日内可申请，发票开具后不可修改且不能退票。 |
| 根因 | 该输入是无问号、无“是什么/怎么办”等谓词的标题式查询。当前确定性问答路由没有把“发票/团队订单/规则”纳入显式票务意图，语义归一也可能把它当普通名词短语交给自由工具检索，导致类别和检索词不稳定。 |
| 当前现状 | 已增加确定性的发票知识计划识别器。明确包含“发票/开票”及团队、订单、申请、修改或退票语义的标题式和问句式输入，直接生成既有 `ticketing + rule/method + brief` 计划，不调用模型决定领域、类别或查询。票务计划现只允许检索 `ticketing_snapshot`，避免基础信息挤占具体票务规则。 |
| 期望状态 | 将标题式输入理解为“请说明团队订单电子发票规则”，限定票务证据后自然回答申请期限、开具后不可修改和退票限制；不展示原文。 |
| 解决方案 | 为审核知识标题式查询增加受控 `rule/method` 意图，加入团队订单、电子发票、开票、申请、修改和退票等主题词；检索查询由代码生成并限定 `ticketing_snapshot`。对这条审核规则不再依赖模型综合：只有证据同时支持“购买后 30 日内、开具后不可修改、不能退票”才生成自然答案；任一条缺失即失败关闭。混有路线规划等其他操作的输入不由此解析器抢占。 |
| 验收 | “团队订单电子发票规则”“团队票怎么开发票”“开票后还能改吗”均命中同一规则组；游览前后答案等价，明确 30 日期限、修改限制和退票限制；不输出原文、文件名、来源编号或类别。无证据或证据不完整时失败关闭，不调用模型补齐。 |
| 自动化验证 | `test_controlled_knowledge_query.py` 覆盖三种问法、类别边界、确定性自然渲染及不完整证据失败关闭；`test_semantic_normalization.py` 覆盖无模型的标题式计划；`test_agent_tour_qa.py` 覆盖游览前后检索参数、答案一致和状态不变。定向 44 项与完整回归 610 项测试均通过；LangSmith 实际链路待复测。 |

### P1-17 `30min` 等中英混合时长没有进入唯一时长解析器

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 用户输入“30min路线，木雕，详细”后，画像收集仍追问“有多少分钟可用于游览”。 |
| 根因 | 当前 `duration_parser` 已覆盖“30分钟、半小时、两小时”等中文表达，但没有把 `min/mins/minute/minutes` 等英文缩写和单位归一为分钟。语义归一候选也未稳定携带该数值，因此 `profile_collection` 收到的时长仍为空。 |
| 当前现状 | `open`。已记录原因，尚未扩展解析器。 |
| 期望状态 | `30min / 30 mins / 30 minutes / 30分钟` 统一解析为 `available_minutes=30`；随后只收集仍缺失的偏好字段，不重复询问时间。 |
| 解决方案 | 只扩展现有 `duration_parser.py` 的单位覆盖和边界正则，并让语义候选复用同一解析结果；保留数值范围、否定表达、日期时间和歧义校验，不另建第二套时间解析。 |
| 验收 | 上述四种表达生成相同的 30 分钟画像；“下午 3:30”“30min 后闭馆”等非路线时长不被误写为游览预算；游览中的剩余时长表达仍走现有确认门控。 |

## 6. P2：经典 / 定制双模式产品协议

### P2-01 当前缺少明确的游览模式事实源

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 当前已有时间、兴趣、详略和扩展画像字段，但没有“经典模式/定制模式”的统一产品协议。不能依靠聊天语气猜测游客想要哪种体验。 |
| 当前现状 | `VisitorProfile`、`GuidancePolicy`、扩展画像选择和 E5 风格素材已经存在；`tour_mode` 尚未成为冻结字段，也未接入路线/讲解策略。 |
| 期望状态 | 开始导览先获取可用时长，再由游客明确选择经典或定制模式。经典模式不主动追问偏好；定制模式只收集必要、可选且明确的偏好。 |
| 解决方案 | 先新增模式契约与状态归属设计，明确 `tour_mode` 属于 VisitorProfile、路线快照还是会话控制；随后才实现 UI/文本选择。不得删除现有 `detail_level`，因为它已参与路线预算与测试；可取消对游客强制询问，改为按模式应用透明默认。 |
| 验收 | 经典模式只靠时长生成代表性路线；定制模式只使用游客明确选择的偏好；两种模式均允许游客主动提问，但不擅自推断身份。 |

### P2-02 定制模式的偏好收集过多或含义重叠

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 若依次询问时间、七类工艺、讲解深浅、人格风格、目的、同行人，容易变成冗长问卷；“简单/深入”又与知识水平、风格和时长部分重叠。 |
| 当前现状 | C5/C8 已有受众、知识水平、表达风格、互动方式的明确选择能力，但真实聊天默认仍是中性画像；没有面向双模式的最小收集流程。 |
| 期望状态 | 定制模式首版最多收集三项：工艺兴趣（多选）、讲解风格、可选的游览目的或同行情况；未填写项保持中性默认。 |
| 解决方案 | 使用多选而不是 127 种工艺组合；把“摄影、研学、亲子”等作为显式目的/同行提示，不从年龄或一句模糊表达推断。风格必须映射到 E5 允许的受控值。 |
| 验收 | 游客可跳过任一可选问题；系统不重复询问已回答字段，也不因“我们”“一起”自动判定家庭或儿童。 |

### P2-03 规划收集期间插入问题后的恢复策略未产品化

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 游客在路线偏好收集过程中可能询问“三顾茅庐是什么意思”；系统应先回答，再恢复到真正缺失的规划字段，而不是卡死在线性问卷。 |
| 当前现状 | 现有 C2/A2 已优先保障问答与控制操作不直接破坏画像收集，但“双模式下的暂停、回答、恢复提示”尚无明确端到端产品验收。 |
| 期望状态 | 收集草稿保留；插入问答不覆盖已确认偏好；回答后只提示下一项必要字段，不强迫重复回答。 |
| 解决方案 | 在模式契约后定义 `profile_collection` 的暂停/恢复规则与验收案例；继续把 VisitorProfile 作为唯一画像事实源。 |
| 验收 | 时间已记录后插入事实问答，再回到收集时只询问模式或下一项缺失偏好。 |

## 7. P2：卡片调度与事件驱动导览

### P2-04 卡片存在，但没有受控的“到点调度”机制

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 术语、比较、研究和打卡卡多数只能由游客直接提问触发，尚未根据当前审核点位、游览模式、兴趣和预算主动进入讲解。 |
| 当前现状 | D1-D6 已完成各类卡的注册、资格门控和问答接入；`StopProgram` 的相关卡片插槽仍未实际调度。E5 契约已规定讲解数据流，但 E5-A/C 尚未完成。 |
| 期望状态 | 到达审核节点后，基础对象事实必选；术语、比较、研究、打卡作为可选增强内容，并受模式、证据、预算、运行资格与现场边界共同限制。 |
| 解决方案 | E5 完成后新增确定性 `CardDispatcher`/调度规则，输入只能来自当前 `node_id`、StopProgram、GuidancePolicy、模式、已明确兴趣与资格注册表。输出为可审计的候选，最终讲解仍需 evidence。 |
| 验收 | 同一状态输入稳定选择相同卡片；无资格/无来源/无映射卡不进入游客文本；卡片调度不修改路线或 TourState。 |

### P2-05 比较卡、研究卡不能默认变成事实或强制插入

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 希望导览有比较思路，但若每件木雕都自动做跨地区比较，会超时、越过证据边界，并把研究观点误说成定论。 |
| 当前现状 | D3/D4 已有研究归因和比较资格门控；八张比较卡目前为 `attributed_only / research_only`。 |
| 期望状态 | 比较卡仅在定制模式明确偏好、专业/研学策略、游客主动比较或本站确有合适证据时，以短小“可选深入说明”出现。研究卡始终以“相关研究指出”表述。 |
| 解决方案 | 在调度器中设定每站至多一个可选比较/研究扩展，并服从 StopProgram 时间预算；普通游客获得基础事实优先。 |
| 验收 | 普通经典路线不因卡片存在而超时；研究/比较内容显示归因、范围与限制。 |

### P2-06 打卡卡需要摄影明确意图和现场限制，不能变成默认推销

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 希望到点提示拍照，但现有打卡卡只是项目编辑候选，不代表热门、馆方推荐、实时开放或可通行机位。 |
| 当前现状 | D5-B/D6 已有编辑候选门控、姿势安全边界和安全拒绝；平台观察永不进入游客内容。当前只在明确拍照请求时读取。 |
| 期望状态 | 仅在游客明确选择摄影目的/偏好，或主动询问拍照时，给出一条非接触、以现场标识和工作人员要求为准的可选提示。经典模式默认不主动插入拍照推荐。 |
| 解决方案 | 将 `photo_preference` 设计为显式、可选的会话偏好；调度器只使用已启用、当前节点关联且安全边界完整的候选。 |
| 验收 | 不选择摄影偏好的经典路线不出现拍照推销；摄影偏好下也不承诺光线、客流、开放或“热门”。 |

## 8. P3：位置事件与主动体验触发

### P3-01 从文本到达升级为可信位置事件

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 真正的主动导览需要知道游客到了哪里；纯聊天文本无法可靠感知物理位置。 |
| 当前现状 | 已有文本 `arrive_at_stop` 和审核节点机制；空间别名冲突尚未关闭。没有 GPS、二维码、蓝牙或地图点击产品接口。 |
| 期望状态 | 先支持“游客点击地图点位/扫描二维码/明确确认到达”这类可信事件；只有确认事件才能触发本站主动讲解和候选提醒。 |
| 解决方案 | 在解决空间数据冲突后，定义 UI 中立的 `location_event` 输入契约；优先 QR 或地图点击，不在当前阶段加入不可靠 GPS 推断。 |
| 验收 | 事件带唯一审核 `node_id`，触发本站内容而不改变路线顺序；错误或未知点位失败关闭。 |

## 9. P3：知识分层与 Adaptive RAG 运行策略

### P3-02 不应让模型自行判定“知道就不检索”

| 项目 | 内容 |
| --- | --- |
| 问题描述 | 直接让 API 模型凭自信决定是否检索，容易对陈家祠事实、展品位置、故事细节或实时服务产生幻觉。 |
| 当前现状 | 项目已有本地混合 RAG、术语/研究/比较/拍照的确定性意图分流，但部分回答质量和路由顺序仍需修复。 |
| 期望状态 | 路线/状态走确定性代码；陈家祠事实和动态服务走本地证据；通用闲聊才可直接调用模型；证据不足时明确说明。 |
| 解决方案 | 在现有 `route_initial_request` 和 Tour QA 上逐步收紧分类与证据阈值，不引入第二套 RAG 或完整知识图谱。若未来要补通用文化故事，应建立可引用的背景资料层，而不是模型记忆层。 |
| 验收 | 任意陈家祠对象/位置/工艺回答可追溯；低相关检索不强答；通用闲聊不污染 TourState。 |

### P3-03 当前不需要把项目重构为完整 KAG

| 项目 | 内容 |
| --- | --- |
| 问题描述 | KAG/知识图谱听起来能解决“多跳关系”，但当前主要障碍是状态、调度、证据组织和空间数据一致性，而不是缺少图数据库。 |
| 当前现状 | 项目已有审核节点图、对象—点位映射、路线图和统一知识卡注册表，已具备后续结构化检索的基础。 |
| 期望状态 | 先用现有结构化数据完成可靠导览；只有出现稳定的多跳关系查询需求时，再评估图谱查询层。 |
| 解决方案 | 暂不引入 OpenSPG/图数据库等大规模重构。将“对象—工艺—点位—证据—卡片”关系保持在当前审核文件与适配器中，并通过调度器消费。 |
| 验收 | 不增加新基础设施也能完成路线、站点讲解、受控卡片调用与来源边界。 |

## 10. P4：暂缓项目

以下能力具有价值，但不应在 P1/P2 未完成前开工：

- GPS、蓝牙信标和视觉识别自动定位；
- 基于长期行为的个性化推荐；
- 跨会话长期游客画像与数据库持久化；
- 实时客流、开放时间、展览或票务同步；
- 多语言完整导览与语音前端；
- 自动生成新知识卡或自动写入空间关联。

它们都要求额外的授权、现场数据、隐私设计或人工审核，不能通过当前聊天/RAG 逻辑安全替代。

## 11. 建议执行顺序

```text
1. 保留 P1-01 的原句回归测试；其路线优先级已在 2026-07-29 通过 LangSmith 复测
2. 在 LangSmith 复测 P1-02、P1-03、P1-05 与 P0-01
3. 由空间数据负责人关闭 P1-04 的权威节点冲突
4. P1-06 已完成实链验证；P1-07 已完成实现和本地索引验证，等待 LangSmith 对照复测
5. 运行 E5 已集成的首站讲解与风格验收；将结果写入其 runtime overlay，不能仅以代码合入视为通过
6. 用 E5 评测关闭讲解深度、重复和计数问题；“详细讲解”仍需单独建立证据化路径
7. 冻结 P2-01 至 P2-03 的经典/定制模式契约
8. 实现受控卡片调度器，先接术语与比较，再接研究与摄影
9. 定义可信位置事件（地图点击/二维码优先）并做主动到点触发
10. 最后评估定位、多模态、长期画像和实时数据能力
```

## 12. 每个任务的统一完成标准

一个问题只有同时满足以下条件才可从问题池关闭：

1. 有清晰的唯一事实源和文件所有权；
2. 不改变 TourState、VisitorProfile、路线和空间数据的既定边界；
3. 有针对原始用户表达的自动化回归测试；
4. 有 LangSmith 新线程与连续线程实测记录；
5. 无效证据、无效节点、审核不足和现场未知条件均安全降级；
6. 更新问题池状态、证据链接和受影响测试；
7. 经负责人确认后才提交、合并或推送公共分支。

## 13. 相关现有材料

- `data/chen_clan_academy/evaluation/manual_reviews/d_stage_review_muziw_v1.yaml`：原始 LangSmith 问题记录；
- `data/chen_clan_academy/evaluation/d_stage_optimization_backlog_v1.yaml`：当前问题池及已有修复状态；
- `E5_NARRATION_CONTRACT.md`：证据驱动讲解的冻结数据流与验收案例；
- `data/chen_clan_academy/narration_styles/styles_v1.yaml`：E5-B 已完成、尚待接入的语言风格素材；
- `TOUR_INTERACTION_CONTRACT.md`：到达、完成、下一站等状态操作边界。
