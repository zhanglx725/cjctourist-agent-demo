# D阶段后团队执行与Codex对接规划 V0

> 状态：预规划草案，尚未冻结。  
> 生效条件：D7完成、完整回归通过并形成统一Git基线。  
> 后续如项目事实、需求或接口变化，应先修订本文件，不能让Codex继续执行过期计划。

---

## 1. 后续总体目标

D阶段完成后，项目已经具备：

```text
路线规划
→ 游览状态管理
→ 到达与讲解
→ 游览中问答
→ VisitorProfile
→ GuidancePolicy
→ 术语/研究/比较/打卡卡片
→ 基础RAG兜底
```

后续重点从“搭建Agent基本能力”转向：

1. 人工验证和系统纠错。
2. 内容与知识质量提升。
3. 主题路线与特色讲解。
4. 用户记忆与持久化。
5. 评估、监控和线程隔离。
6. UI/API与演示产品化。
7. 项目答辩、演示和部署准备。

---

## 2. 三方角色

### 负责人A：内容与体验工作流

主要负责：

- 人工体验测试。
- 卡片事实、来源和空间映射审核。
- RAG更新候选。
- 语言风格库。
- 主题路线内容。
- 儿童、家庭、研学、专业表达。
- 拍照、观察和互动任务。
- 人物、工艺传承与故事内容。

建议分支：

```text
codex/content-experience
```

### 负责人B：平台与产品化工作流

主要负责：

- 持久化记忆。
- Checkpointer升级。
- 线程与用户隔离。
- LangSmith评估集。
- 错误监控和性能。
- API与展示协议。
- UI或演示界面。
- 部署和端到端测试。

建议分支：

```text
codex/platform-productization
```

### 集成负责人

主要负责：

- 冻结公共契约。
- 维护公共Agent代码。
- 判断规划—实现冲突。
- 审核共享文件修改。
- 合并队友分支。
- 运行完整回归。
- 统一更新项目进度和答辩材料。
- 决定优化优先级。

建议分支：

```text
main
codex/integration
```

队友原则上不直接修改 `main`。

---

## 3. 长期不变的架构边界

所有Codex开始任务前必须确认：

1. VisitorProfile是游客期望的唯一运行事实源。
2. TourState是本次游览实际状态的唯一事实源。
3. TourState只能通过冻结的确定性事件适配层修改。
4. 到达不等于完成。
5. 知识问答、研究、比较和拍照建议不能修改路线进度。
6. StopProgram是本点讲什么的执行方案，不是第二份画像。
7. GuidancePolicy是VisitorProfile的确定性派生结果。
8. 基础RAG负责稳定、通用事实。
9. 研究、比较、术语、打卡等知识卡按类型调用，不批量塞入一个大RAG。
10. 空间节点、路线边和对象映射只能使用审核数据。
11. 平台观察、未审核内容和模型推测不能变成事实。
12. 不能根据聊天语气推断儿童、老人、专家或家庭身份。
13. 每个 `thread_id` 的状态必须隔离。
14. 任何新功能都必须具备数据异常时的安全回退。

---

# 第一阶段：E——人工验证与问题收敛

## E1：冻结测试基线

集成负责人负责：

1. 确认D7提交。
2. 记录基线提交哈希。
3. 运行完整回归。
4. 冻结D阶段验收案例。
5. 将同一提交提供给两位队友。

输出：

```text
D_STAGE_BASELINE.md
```

包含：

```text
基线提交
测试数量
Python环境
LangSmith场景
已知限制
允许优化范围
```

没有统一基线，不得开始多人测试。

## E2：两位队友独立人工测试

两位队友按照：

```text
POST_D_TEAM_EVALUATION_AND_OPTIMIZATION_STANDARD.md
```

分别提交：

```text
d_stage_review_<reviewer>_v1.yaml
```

重点测试：

- 路线和时间。
- 状态变化。
- 站点讲解。
- RAG事实。
- 术语。
- 研究观点。
- 比较。
- 拍照。
- VisitorProfile。
- 连续对话。
- 线程隔离。
- 安全回退。

本阶段只能记录问题，不能各自直接修改公共Agent代码。

## E3：问题去重与确认

集成负责人建立：

```text
d_stage_optimization_backlog_v1.yaml
```

处理：

```text
相同问题合并
→ 无法复现问题退回
→ 判断数据/代码/契约层
→ 标记P0/P1/P2/P3
→ 指定负责人
→ 制定验收测试
```

任何优化项必须先进入公共问题池，才能开始实现。

## E4：第一轮正确性修复

只修：

```text
P0
P1
```

优先级：

```text
状态与安全
→ 事实与来源
→ 路由
→ 空间映射
→ 卡片选择
→ 回退
```

每个修复单独或按同一原因组成小批次，不能把大量不相关问题放入一个提交。

## E5：内容质量优化

处理P2：

- 讲解太长或太短。
- 重复内容。
- 当前点重点不突出。
- 个性化不明显。
- 研究回答过于学术。
- 比较缺少清晰维度。
- 拍照建议不够自然。
- 来源展示影响阅读。

## E6：体验优化

处理P3：

- 语言节奏。
- 按钮文案。
- 回答排版。
- 提示语。
- 演示美观。
- 非关键性能。

---

# 第二阶段：F——知识、表达与主题路线

主要由内容与体验负责人推进。

## F1：RAG更新候选审核

将测试和卡片整理中发现的稳定事实分类：

```text
进入基础RAG
保留知识卡
需要进一步来源
拒绝使用
```

进入基础RAG的内容必须：

- 有可靠来源。
- 属于通用稳定事实。
- 不只是论文观点。
- 不只是拍照建议。
- 不只是平台线索。
- 不与现有知识冲突。

流程：

```text
提交候选
→ 人工复核
→ 更新knowledge文件
→ 重建索引
→ RAG定向测试
→ 完整回归
```

## F2：人物与传承故事卡

建立人物、工匠和传承故事时，必须区分：

```text
馆方确认事实
论文或采访观点
口述材料
民间传说
团队改写
```

不得把人物故事直接混入基础RAG。

建议字段：

```text
story_id
person_name
identity
time_period
story_type
summary
source_ids
fact_boundary
applicable_node_ids
audience_modes
review_status
```

## F3：讲解风格库

建议目录：

```text
data/chen_clan_academy/guidance_styles/
```

首批风格：

```text
child_friendly
family
study
professional
mixed_group
```

风格库只改变表达，不增加事实。

每份风格资源规定：

- 开场方式。
- 句子长度。
- 词汇难度。
- 故事比例。
- 提问方式。
- 互动任务形式。
- 禁止表达。
- 事实占位符。

所有事实仍来自RAG或审核卡片。

## F4：主题线路契约

主题线路不能建立第二套空间图或路线算法。

主题只提供：

```text
主题偏好
候选节点
候选对象
讲解重点
知识卡类型
排序权重
```

实际路线仍由现有路线规划器根据：

```text
时间预算
审核空间图
步行成本
讲解预算
已访问状态
```

生成。

建议主题：

### 主题一：灰塑工艺精华线

重点：

- 灰塑工艺。
- 独角狮。
- 吉祥题材。
- 屋脊装饰。
- 工艺与空间位置。

### 主题二：岭南雕塑工艺对比线

重点：

- 灰塑。
- 石雕。
- 砖雕。
- 木雕。
- 材料、工艺和表现差异。

### 主题三：吉祥题材故事线

重点：

- 福禄寿。
- 功名富贵。
- 松鹤延年。
- 瑞兽与植物寓意。
- 传说和事实边界。

### 主题四：建筑空间与礼制线

重点：

- 前院。
- 月台。
- 聚贤堂。
- 庭院层次。
- 建筑空间、仪式与宗族功能。

### 主题五：亲子观察互动线

重点：

- 找图案。
- 找颜色。
- 找动物。
- 安全、非接触式观察任务。
- 简短故事。

### 主题六：研学与学术线

重点：

- 研究摘要卡。
- 比较卡。
- 术语卡。
- 研究方法。
- 观点与限制。

### 主题七：摄影打卡体验线

重点：

- 审核节点。
- 编辑推荐构图。
- 安全姿势。
- 家庭、个人与合影。
- 不承诺光线、客流和开放状态。

## F5：主题路线数据

建议建立：

```text
data/chen_clan_academy/routes/theme_profiles_v1.yaml
```

每个主题：

```yaml
theme_id:
title_zh:
interest_tags: []
preferred_node_ids: []
preferred_object_ids: []
preferred_card_types: []
narration_focus: []
audience_modes: []
limitations: []
review_status:
```

`preferred_node_ids`只是规划候选，不是固定路线。

## F6：主题路线接入

用户示例：

```text
“我有45分钟，想重点看灰塑。”
“给孩子规划一条找动物图案的路线。”
“我想比较几种雕刻工艺。”
“给我一条适合拍照的路线。”
```

流程：

```text
主题识别
→ VisitorProfile确认
→ 时间预算
→ 主题权重
→ 现有路线规划器
→ StopProgram
→ 对应知识卡
```

不得根据主题直接拼接未经路线规划器校验的节点顺序。

---

# 第三阶段：G——记忆与平台能力

主要由平台与产品化负责人推进。

## G1：记忆契约冻结

先区分：

### 会话记忆

包括：

- messages
- VisitorProfile
- TourState
- active_route_plan
- active_stop_program
- 当前问答上下文

### 持久化偏好

可以包括：

- 用户明确保存的兴趣。
- 常用讲解深度。
- 语言偏好。
- 互动偏好。

不得长期保存：

- 未经同意推断的身份。
- 不必要的位置历史。
- 敏感个人信息。
- 模型猜测。

## G2：持久化Checkpointer

Demo建议优先：

```text
SQLite
```

目标：

- 程序重启后线程可恢复。
- 不同线程隔离。
- 数据可删除。
- 测试环境可重置。
- 不改变AgentState字段语义。

不能把数据库状态建立成TourState之外的第二份游览事实。

## G3：画像持久化

流程：

```text
用户明确同意保存
→ 持久化偏好
→ 新会话初始化VisitorProfile
→ 本次会话继续以VisitorProfile为运行事实源
```

持久化记录只是会话初始化来源，不能在运行中与VisitorProfile竞争。

需要：

- 同意。
- 查看。
- 修改。
- 删除。
- TTL或过期策略。
- 用户隔离。

## G4：长对话摘要

只有对话较长后才生成摘要。

摘要保留：

- 明确偏好。
- 已确认路线。
- 当前游览状态。
- 尚未解决的问题。

摘要不得覆盖结构化TourState或VisitorProfile。

## G5：记忆测试

覆盖：

- 相同thread恢复。
- 不同thread隔离。
- 程序重启恢复。
- 删除后无法恢复。
- 用户A不能读取用户B数据。
- 摘要不改变状态。
- 数据库异常安全降级。

---

# 第四阶段：H——评估、UI与产品化

主要由平台与产品化负责人推进。

## H1：LangSmith评估集

建立固定数据集：

- 路线规划。
- 游览状态。
- 点位问答。
- RAG。
- 术语。
- 研究。
- 比较。
- 拍照。
- 画像。
- 长对话。

指标：

```text
路由正确率
事实正确率
来源完整率
状态违规率
卡片选择正确率
安全回退率
回答长度
响应时间
```

状态违规率目标必须为0。

## H2：评估自动化

每次关键合并后：

```text
单元测试
→ 集成测试
→ LangSmith评估
→ 人工抽查
```

评估结果记录提交哈希，避免不同版本结果混淆。

## H3：API与展示协议

UI不直接操作TourState。

所有按钮转换为冻结事件：

```text
到达
讲解结束
确认完成
下一站
跳过
修改时间
结束路线
```

知识问答和拍照推荐为只读操作。

展示层需要区分：

- 导航提示。
- 站点讲解。
- RAG回答。
- 研究观点。
- 比较结果。
- 拍照建议。
- 澄清。
- 错误和安全回退。

## H4：演示界面

Demo至少展示：

- 游客画像收集。
- 路线生成。
- 路线节点。
- 到达和讲解。
- 追问。
- 比较。
- 拍照推荐。
- 游览进度。
- 来源信息。
- 结束总结。

## H5：部署与演示脚本

准备：

- 环境安装说明。
- 配置文件模板。
- RAG索引初始化。
- 数据库初始化。
- 启动命令。
- 健康检查。
- 演示账号或线程。
- 失败回退方案。
- 答辩演示流程。

---

# 第五阶段：I——游览闭环与扩展功能

该阶段为建议规划，D7后需重新对照需求文档确认。

候选能力：

## I1：游览完成总结

输出：

- 已访问点位。
- 重点知识。
- 游客兴趣回顾。
- 推荐继续了解的主题。
- 来源与学习建议。

不能虚构游客实际看到的对象。

## I2：成就与收集

可设计：

- 已完成主题。
- 已观察对象。
- 已学习术语。
- 已完成安全互动任务。

成就只来自真实事件和已确认完成状态。

## I3：告别与后续推荐

包括：

- 参观总结。
- 未完成点位。
- 下次主题路线。
- 知识回顾。
- 删除或保存偏好选择。

## I4：周边或实时信息

如涉及：

- 开放时间。
- 票务。
- 交通。
- 临时展览。
- 天气。
- 周边服务。

必须使用独立、可更新的数据源，不能依赖静态RAG猜测。

---

# 4. 两个Codex的标准执行步骤

每个任务都按以下顺序执行。

## 步骤1：同步基线

开始前：

```text
确认分支
拉取最新基线
记录提交哈希
检查工作区
阅读交接文档
```

发现未提交修改时不得覆盖。

## 步骤2：阅读必需文件

Codex必须先阅读：

- 任务计划。
- 协作指南。
- 进度报告。
- 相关冻结契约。
- 相关代码。
- 相关数据。
- 相关测试。

不得只根据用户提示猜测项目现状。

## 步骤3：复述边界

修改前简要说明：

```text
本任务做什么
不做什么
允许修改哪些文件
状态是否允许变化
事实来自哪里
如何验证
```

## 步骤4：检查规划—实现冲突

发现以下情况立即暂停：

- 文档和代码语义不同。
- 数据字段无法支持计划。
- 必须修改其他成员文件。
- 必须改变冻结契约。
- 测试要求与真实行为冲突。
- 需要增加第二份事实源。
- 未审核内容必须被当成事实才能实现。

报告格式：

```text
## 发现规划—实现冲突

任务：
冲突内容：
现有实现：
规划要求：
影响文件：
影响数据：
最小兼容方案：
推荐方案：
是否需要修改契约：
```

未经集成负责人确认，不得自行绕过。

## 步骤5：最小实现

- 只修改任务范围内文件。
- 优先复用已有模块。
- 不建立近义重复模块。
- 不批量重构无关代码。
- 不自行放宽审核和安全门控。
- 不自行更改ID。

## 步骤6：测试

按顺序：

```text
新增单元测试
→ 模块测试
→ 相关集成测试
→ 完整回归
→ 静态格式检查
→ LangSmith或人工场景
```

如果Codex环境无法运行项目虚拟环境：

- 给出用户本机完整命令。
- 标记待本机验证。
- 不得说环境已经损坏。
- 不得把未执行测试报告为通过。

## 步骤7：更新必要文档

每个任务只更新：

- 本模块README。
- 必要的进度记录。
- 必要的协作边界。
- 必要的学习与答辩说明。

不要在多个文件复制同一大段设计。

## 步骤8：本机验收

用户或负责人运行测试，将结果发回Codex。

测试未通过时不得进入下一阶段。

## 步骤9：提交与上传

本机验证通过后：

```text
git status
git diff --check
git add 指定文件
git commit
git push 当前任务分支
```

每个阶段至少形成一个可独立回退的提交。

上传后必须报告：

```text
分支
提交哈希
提交说明
测试数量
测试结果
影响文件
已知限制
下一步
```

禁止：

- `git push --force`
- 覆盖其他成员提交
- 未经验证直接推送
- 把多个不相关阶段合成一个提交

## 步骤10：交接

每个任务提交：

```text
TASK_HANDOFF_<TASK_ID>.md
```

包含：

```text
任务编号
负责人
基线提交
完成内容
未完成内容
修改文件
新接口
数据变化
测试命令
测试结果
LangSmith结果
已知问题
规划—实现冲突
下一任务前置条件
分支和提交哈希
```

---

# 5. 合并流程

队友推送任务分支后，集成负责人：

```text
读取交接
→ 检查文件所有权
→ 拉取最新main
→ 重新基线化
→ 合并
→ 解决冲突
→ 定向测试
→ 完整回归
→ LangSmith
→ 更新公共进度
→ 推送main
```

不能仅依据队友分支测试结果宣布主分支通过。

---

# 6. 推理模式建议

### 轻

用于：

- 格式修正。
- README更新。
- 测试数据整理。
- 已确定规则的机械实现。
- Git状态检查。

### 中

用于：

- 普通功能接入。
- 数据校验。
- 卡片检索。
- 主题路线。
- UI/API。
- 测试设计。

### 高

仅用于：

- TourState语义。
- VisitorProfile与持久化边界。
- 路由架构冲突。
- 记忆事实源。
- 安全或隐私。
- 多模块契约修改。
- 无法通过现有设计消除的冲突。

不得每个任务默认使用高推理。

---

# 7. 大优化执行原则

人工测试后不能马上开始“大改”。

必须先：

```text
问题收集完成
→ 去重
→ 复现
→ 分层
→ 排优先级
→ 确认负责人
→ 锁定验收测试
```

一轮优化建议最多聚焦一个主题：

```text
状态与路由
内容与来源
个性化与风格
记忆与线程
界面与性能
```

每轮优化结束后：

- 所有原测试通过。
- 新问题有回归测试。
- 人工复验通过。
- 文档同步。
- 独立提交和上传。
- 再进入下一轮。

---

# 8. 本规划的修订规则

本文件当前是V0预规划。

以下情况发生时必须修订：

- D阶段真实实现与规划不同。
- 需求文档改变。
- 文件所有权改变。
- 两位队友实际卡片范围改变。
- 记忆或UI技术方案确定。
- 部署环境确定。
- 新增外部数据源。
- 主题路线在测试中不可行。

D7完成后应生成：

```text
POST_D_TEAM_EXECUTION_AND_HANDOFF_PLAN_V1.md
```

V1需要记录：

```text
修订依据
删除的任务
新增的任务
阶段顺序变化
负责人变化
接口变化
测试变化
```

V1确认后冻结，所有Codex以V1为准，V0仅保留历史参考。